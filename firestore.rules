rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /plankEntries/{entryId} {
      allow read, write: if request.auth != null;
    }
    match /pushupEntries/{entryId} {
      allow read, write: if request.auth != null;
    }
  }
}
